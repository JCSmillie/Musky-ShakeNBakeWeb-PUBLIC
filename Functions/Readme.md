# Functions
All of the scripts in this folder provide extended functions to Musky.  Here is an explaination of each file:
* ConvertLoanerData.py <-Processing script to convert CSV data that comes back from MOSBasic for web display.
* IIQ_Support_Functions.sh <-Functions to connect to IIQ.  NOTE while the methods are universal any explicit calls to group IDs will be different then what is in your own install.
* JSONPath.sh <-
* LoanerData.sh <-Runs through the Loaner page.  This is what processes the request.
* LocationWebLink.sh <-Use for Lost Mode to open a webpage to Apple Maps and show location of lsot device.
* Mosyle_Support_Functions.sh <-Used by the Device Manager page.
* WipeiPadandUpdateinventory.sh <-Used by the 3rd Party Device Retirement module.
